server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medical_clinic_db
    username: postgres
    password: root
    driver-class-name: 'org.postgresql.Driver'
  jpa:
    show-sql: true
#    hibernate:
#      ddl-auto: create